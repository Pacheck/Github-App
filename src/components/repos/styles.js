import styled from 'styled-components';

const StyledRepos = styled.div`
  margin: 0 auto;
  display: flex;
  flex-wrap: wrap;
  padding: 20px;

  div {
    margin: 0 auto;
  }
  li {
    list-style: none;
  }
`;

export default StyledRepos;
